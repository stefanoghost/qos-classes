# CKA Practice - Labels and Selectors

In this scenario you will practice Kubernetes **labels** and **selectors**.

Labels are key-value pairs attached to Kubernetes objects.

Selectors are used by resources such as Services to find matching Pods.

A Service can exist and still not route traffic if its selector does not match any Pod labels.
