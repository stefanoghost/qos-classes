# Scenario Completed

Great job!

You learned how to expose applications using:

- ClusterIP → internal communication inside the cluster
- NodePort → external access from outside the cluster

## Key concept

- ClusterIP = internal only
- NodePort = accessible from outside the cluster via node IP + port
