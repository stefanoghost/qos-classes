# Ingress Configuration with TLS

In this scenario you will create an Ingress resource for an API service.

You will configure:

- hostname routing
- TLS
- path-based routing
- backend service forwarding

The TLS secret already exists.
