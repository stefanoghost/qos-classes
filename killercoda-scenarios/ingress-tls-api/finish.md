# Scenario Completed

Well done.

You created an Ingress with:

- host-based routing
- TLS configuration
- path-based backend rules

## Key concept

TLS is configured at the Ingress level.

The backend Service receives normal HTTP traffic after TLS termination at the Ingress controller.
