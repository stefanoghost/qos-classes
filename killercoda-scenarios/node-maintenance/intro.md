# Node Maintenance Scenario

A Kubernetes node requires maintenance and must be safely prepared.

You will:

- prevent new pods from being scheduled
- safely evict running workloads
- preserve critical data

This scenario simulates a real production maintenance operation.
