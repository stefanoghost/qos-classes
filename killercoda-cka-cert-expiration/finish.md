# Completed

You successfully checked the `kube-apiserver` certificate expiration date and wrote the correct `kubeadm` renewal command.

This is a realistic CKA-style task because it tests practical knowledge of:

- Kubernetes PKI file locations
- `openssl` certificate inspection
- `kubeadm certs check-expiration`
- `kubeadm certs renew`
